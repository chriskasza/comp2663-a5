<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chrisneric.videorentalsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AccountsActivity" />
        <activity android:name=".activity.NewAccountActivity" />
        <activity android:name=".activity.AccountActivity" />
        <activity android:name=".activity.InventoryActivity" />
        <activity android:name=".activity.MovieActivity" />
        <activity android:name=".activity.NewMovieActivity" />
        <activity android:name=".activity.RentalsActivity" />
        <activity android:name=".activity.NewRentalActivity" />
        <activity android:name=".activity.PaymentActivity" />
        <activity android:name=".activity.BaseActivity"></activity>
    </application>

</manifest>